import 'package:flutter/material.dart';

class QuotidiennePage extends StatelessWidget {
  const QuotidiennePage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text("Page Quotidienne"),
    );
  }
}